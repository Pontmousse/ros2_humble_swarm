---
- name: Provision RoboMaster system
  hosts: robots
  become: yes

  vars_files:
    - vars.yml

  tasks:

    - name: Prepare robot system
      ansible.builtin.include_tasks: tasks/system_setup.yml
